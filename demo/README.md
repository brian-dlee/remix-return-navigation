# Remix Return Navigation Example

## Run the example

From your terminal:

```sh
npm run dev
```

The app itself documents itself, so after you have the app running and open it up it should be pretty easy to use.
